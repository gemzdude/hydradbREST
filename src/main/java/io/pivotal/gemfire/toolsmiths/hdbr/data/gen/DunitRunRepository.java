package io.pivotal.gemfire.toolsmiths.hdbr.data.gen;

import com.nurkiewicz.jdbcrepository.JdbcRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcOperations;
import org.springframework.stereotype.Repository;
import org.slf4j.LoggerFactory;
import org.slf4j.Logger;
import io.pivotal.gemfire.toolsmiths.hdbr.data.gen.db.DunitRunDB;
import io.pivotal.gemfire.toolsmiths.hdbr.data.gen.DunitRun;
import org.springframework.jdbc.core.RowMapper;
import com.nurkiewicz.jdbcrepository.RowUnmapper;

/**
 * This class is generated by Spring Data Jdbc code generator.
 *
 * @author Spring Data Jdbc Code Generator
 */
@Repository
public class DunitRunRepository extends JdbcRepository<DunitRun, Integer>
{

	final static Logger logger = LoggerFactory.getLogger (DunitRunRepository.class);

	@Autowired
	private JdbcOperations jdbcOperations;

	protected JdbcOperations getJdbcOperations()
	{
		return this.jdbcOperations;
	}

	public DunitRunRepository()
	{
		super (DunitRunDB.ROW_MAPPER, DunitRunDB.ROW_UNMAPPER, DunitRunDB.getTableName ());
	}

	public DunitRunRepository(RowMapper<DunitRun> rowMapper, RowUnmapper<DunitRun> rowUnmapper, String idColumn)
	{
		super (DunitRunDB.ROW_MAPPER, DunitRunDB.ROW_UNMAPPER, DunitRunDB.getTableName (), idColumn);
	}

	@Override
	protected DunitRun postCreate(DunitRun entity, Number generatedId)
	{
		entity.setId(generatedId.intValue());
		entity.setPersisted(true);
		return entity;
	}



	/* START Do not remove/edit this line. CodeGenerator will preserve any code between start and end tags.*/

	/* END Do not remove/edit this line. CodeGenerator will preserve any code between start and end tags.*/

}

